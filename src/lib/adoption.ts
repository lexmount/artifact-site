"use client";
// Claims require an explicit destination tenant. Retained for callers that refresh personal shelves.
export async function adoptStoredSites(): Promise<number> { return 0; }
export function __resetAdoptionForTests(): void { /* no cached automatic claim */ }
